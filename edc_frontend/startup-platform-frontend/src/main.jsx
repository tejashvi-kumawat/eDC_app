import React from 'react'
import ReactDOM from 'react-dom/client'
import StartupPlatform from './App.jsx'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <StartupPlatform />
  </React.StrictMode>,
)
